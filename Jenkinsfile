pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Static Web App - No Build Needed'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Ready to deploy'
            }
        }
    }
}
